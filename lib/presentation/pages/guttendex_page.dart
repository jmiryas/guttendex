import 'package:flutter/material.dart';

class GuttendexPage extends StatelessWidget {
  const GuttendexPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Guttendex"),
        centerTitle: true,
      ),
    );
  }
}
